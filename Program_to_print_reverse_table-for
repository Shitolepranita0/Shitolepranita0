#include<stdio.h>
#include<conio.h>
int main()
{
  int no=0,cnt=0;
printf("\n\t enter number for print its reverse table\n");
scanf("%d",&no);
  for(cnt=10;cnt>=1;cnt--)
  {
    printf("\n\t%d",no*cnt);
  }
getch();
return 0;
}
